
name: CI

on:
  push:
  workflow_dispatch:
    inputs:
      environment:
        type: environment
        description: Select the environment
      
        options:
        - LMDA
        - LMDB
        
jobs:
  build:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
  
    steps:
     
      - name: code checkout
        uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo {{ secrets.DB_HOSTNAME }}
      
